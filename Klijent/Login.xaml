<Window x:Class="Klijent.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login"
        Height="180" Width="265"
        WindowStartupLocation="CenterScreen">
    <Grid Background="#f0f0f0">

        <!-- Kartica u sredini prozora -->
        <Border Width="260"
                Padding="16"
                Background="White"
                CornerRadius="6"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <StackPanel>

                <TextBlock Text="Login"
                           FontSize="16"
                           FontWeight="Bold"
                           Margin="0,0,0,10"
                           HorizontalAlignment="Center"/>

                <!-- Mreža za label + textbox -->
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="korisnicko ime:"
                               Grid.Row="0" Grid.Column="0"
                               Margin="0,0,10,5"
                               VerticalAlignment="Center"/>

                    <TextBox x:Name="txt_korisnicko_ime" Grid.Row="0" Grid.Column="1"
                             Height="22"
                             />

                    <TextBlock Text="lozinka:"
                               Grid.Row="1" Grid.Column="0"
                               Margin="0,0,10,0"
                               VerticalAlignment="Center"/>

                    <!-- može i TextBox umesto PasswordBox ako hoćeš -->
                    <PasswordBox x:Name="txt_lozinka" Grid.Row="1" Grid.Column="1"
                                 Height="22"/>
                </Grid>

                <!-- Dugmad ispod, centrirana -->
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            Margin="0,10,0,0">
                    <Button x:Name="LoginButton" Content="uloguj se"
                            Width="90"
                            Margin="0,0,10,0" Click="Login_Click"/>
                    <Button Content="registruj se"
                            Width="90" Click="Register_Click"
                            />
                </StackPanel>

            </StackPanel>
        </Border>
    </Grid>
</Window>
